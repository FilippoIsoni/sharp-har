# notebooks/c3_ft — the C3-ft session runner

One pinned runner for the 13th pre-registered test row (approved
2026-07-19): CE fine-tuning of C3's SupCon encoder. The recipe, its
rationale and the a-priori hypothesis live in `configs/c3_ft.yaml`'s
header and `splits/CHANGELOG.md` (2026-07-19 entry) — this folder only
holds the ready-to-run copy of the `03_train` template (RUN pinned, GPU
sanity cell), same pattern as `e1_seed_replicates/`.

Executed copy -> `notebooks/runs/YYYY-MM-DD_c3_ft.ipynb` + STATUS line,
same commit. This template stays output-free.
